export function formatDate(dateStr) {
    if (!dateStr) return '-';
    // Dummy formatter for now, since data is already in YYYY-MM-DD or formatted manually
    return dateStr;
}

export function formatCurrency(amountStr) {
    if (!amountStr) return '-';
    return amountStr;
}
